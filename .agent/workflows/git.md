---
description: Git管理（ステータス確認、追加、コミット、プッシュ）
---

# /git — 簡素化されたGit操作

リポジトリの状態を最新に保つための基本操作。

## 手順

// turbo-all

1. `git status` で変更内容を最終確認。
2. `git add .` で全変更をステージング。
3. `git commit -m "[AUTOMATED] Documentation & Workflow Optimization"`
4. `git push` を実行。
5. `git log -n 1` で結果を表示。
